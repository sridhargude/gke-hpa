# Generic
.DS_Store
# macOS Finder
Icon\r
# CR/LF
# Thumbnails, editor backup files
*~
# Environment files that may contain secrets
.env
.env.*

# Logs
*.log

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
.python-version
.venv/
venv/
env/
ENV/
pip-wheel-metadata/
.mypy_cache/
.pytest_cache/

# Docker
# ignore image build artifacts and compose overrides
docker-compose.override.yml
docker-compose.*.yml

# Kubernetes / local clusters
.kube/
kubeconfig

# Terraform
# Local state, crash log, override files, CLI configs
.terraform/
*.tfstate
*.tfstate.*
terraform.tfstate.backup
terraform.tfvars
terraform.tfvars.json
*.tfvars
override.tf
override.tf.json
crash.log
.terraform.lock.hcl
*.tfplan
terraform.tfplan
terraform.rc
.terraformrc

# Terraform workspace files
.terraform.d/

# Helm
# Packaged charts and dependency charts generated by helm
# Ignore tarballs created by `helm package`
**/charts/*.tgz
**/charts/
# Helm repository cache & local generated files
.helm/
helmfile.d/
# chart.lock files generated by dependency update (optional to ignore)
Chart.lock
chart.lock

# Helm release manifests (if produced by CI or scripts)
release-*.yaml

# Packaged app artifacts
*.tgz

# Scripts / generated artifacts
/*.bak
*.backup

# Terraform state in terraform folder (some workflows use terraform/ subfolder)
terraform.tfstate*

# Ignore user-specific files
.idea/
*.sublime-workspace
*.sublime-project

# Ignore compiled binaries
*.exe
*.dll
*.so
*.dylib

# Local test output
/.pytest_cache/

# Keep Helm templates and chart sources tracked (templates/ kept in repo)
